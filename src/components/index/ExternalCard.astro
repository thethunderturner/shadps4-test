---
const {href, aria, icon: Icon, primary_color, secondary_color, name, tag, description, button} = Astro.props;
---

<a
    href={href}
    class="flex flex-col justify-between group rounded-xl p-6 bg-compat-card border border-border shadow-md transform hover:transition ease-in-out hover:-translate-y-1 hover:shadow-lg text-text"
    aria-label={aria}
>
    <div class="flex flex-col">
        <div class="flex items-center gap-3">
            <div class="rounded-full p-2 text-2xl" style={`background-color: ${primary_color};`}>
                {Icon && <Icon className="size-8" style={secondary_color ? {color: secondary_color} : {}} />}
            </div>
            <div>
                <h3 class="text-lg font-bold">{name}</h3>
                <p class="text-sm">{tag}</p>
            </div>
        </div>
        <p class="mt-4 text-sm">
            {description}
        </p>
    </div>

    <div>
        <div class="mt-6 flex items-center gap-3">
            <span class="ml-auto inline-flex items-center gap-2 rounded-md border border-border bg-root px-3 py-1 text-xs">
                {button}
            </span>
        </div>
    </div>
</a>
